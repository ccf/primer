apiVersion: v1
kind: Service
metadata:
  name: {{ include "primer.fullname" . }}-frontend
  labels:
    {{- include "primer.labels" . | nindent 4 }}
    {{- include "primer.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "primer.frontend.selectorLabels" . | nindent 4 }}
